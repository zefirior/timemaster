import os
import src

PACKAGE_DIR = os.getcwd()
SRC_DIR = PACKAGE_DIR + '/src'
CONTENT_DIR = PACKAGE_DIR + '/content'
AUDIO_DIR = CONTENT_DIR + '/audio'
FILE_DIR = CONTENT_DIR + '/file'
IMAGE_DIR = CONTENT_DIR + '/image'

# print(PACKAGE_DIR, SRC_DIR, CONTENT_DIR, AUDIO_DIR)
